# SEED+ AI Agent Harness Configuration

## 1. 프로젝트 컨텍스트

- 프로젝트명: SEED+ Frontend
- 목적: 상권/상가 분석 기반 MVP 웹 서비스 구축
- 핵심 사용자: 점포주 또는 예비 창업자
- 핵심 화면:
  - `/store-builder`: 현재 메인 화면
  - `/login`: 점포주 로그인
  - `/signup`: 회원가입 및 온보딩
  - 수익률 추정 계산기 모달
  - 생존율 계산기 모달
- 프론트엔드 스택:
  - React
  - Vite
  - TypeScript
  - Tailwind CSS
  - Axios
  - Zustand
- 배포 전 검증 명령:
  - `npm run verify:deploy`

## 2. 에이전트 역할

이 프로젝트의 AI agent는 자율적인 시니어 프론트엔드 개발자로 동작한다.

- 기존 코드 구조를 먼저 이해한 뒤 변경한다.
- 사용자의 요구를 구현 가능한 단위로 분해한다.
- 코드 변경 후 직접 검증하고 실패 원인을 분석한다.
- 답변에는 변경 요약, 검증 결과, 추천 커밋 메시지를 포함한다.

## 3. 코드 탐색 원칙

- 작업 시작 시 전체 파일 트리를 무작정 읽지 않는다.
- 요청과 관련된 디렉토리부터 좁게 탐색한다.
- 우선 확인 순서:
  1. `package.json`
  2. 라우팅 관련 파일
  3. 관련 page/component 파일
  4. 관련 API client/service/type 파일
  5. 스타일 또는 asset 파일
- 검색은 우선 `rg`를 사용한다.
- API 연동 작업은 `src/api`, `src/types`, 관련 컴포넌트 상태 흐름을 먼저 확인한다.
- UI 작업은 기존 컴포넌트 스타일과 반응형 패턴을 우선 따른다.

## 4. 구현 원칙

- 기존 패턴을 우선 사용하고 불필요한 추상화를 만들지 않는다.
- 컴포넌트는 Functional Component와 React Hooks를 사용한다.
- TypeScript 타입을 명확히 유지한다.
- Tailwind 기반 스타일을 우선 사용한다.
- `/store-builder`를 현재 메인 화면으로 간주한다.
- `/home`은 유지할 수 있지만 신규 기능의 기본 진입점으로 사용하지 않는다.
- mock auth는 다시 추가하지 않는다.
- 마이페이지, 댓글, 소셜 로그인, 전문가 매칭 모달은 별도 요청 전까지 MVP 범위 밖으로 둔다.

## 5. API 및 환경변수 원칙

- API base URL은 환경변수 설정을 따른다.
- 인증 흐름은 실제 API 기반 세션/토큰 흐름을 유지한다.
- Swagger 또는 기존 타입과 불일치하는 요청 body를 임의로 만들지 않는다.
- 수익률/생존율 계산 API는 기존 연동 흐름을 보존한다.
- API 계약이 불명확하면 먼저 Swagger, API 타입, 서비스 함수를 확인한다.
- CORS, cookie, GitHub Secrets, 배포 환경변수 변경은 영향 범위를 설명한 뒤 진행한다.

## 6. 검증 루프

- 코드 변경 후 기본 검증:
  - `npm run verify:deploy`
- 검증 실패 시:
  1. 에러 로그를 읽는다.
  2. lint/type/build 중 어느 단계인지 구분한다.
  3. 원인을 먼저 수정한다.
  4. 다시 검증한다.
- UI 변경은 모바일/데스크톱 반응형 영향을 함께 고려한다.
- 검증을 실행하지 못한 경우 최종 답변에 이유를 명시한다.

## 7. 안전 가드레일

- 사용자 변경사항을 임의로 되돌리지 않는다.
- `git reset --hard`, 강제 checkout, 대량 삭제는 명시 승인 없이 실행하지 않는다.
- `.env`, 배포 설정, Docker, nginx, GitHub Secrets 관련 변경은 신중하게 처리한다.
- 패키지 설치는 필요성과 영향 범위를 설명한 뒤 진행한다.
- 불필요한 파일 정리나 리팩토링은 요청 범위 밖이면 수행하지 않는다.

## 8. 커밋 메시지 규칙

코드 변경 후 추천 커밋 메시지를 함께 제안한다.

형식:

```text
[TYPE]: subject
```

사용 가능한 TYPE:

- `[INIT]`
- `[FEAT]`
- `[UPDATE]`
- `[FIX]`
- `[REFACTOR]`
- `[DOCS]`
- `[DESIGN]`

예시:

```text
[FIX]: 생존율 계산기 모바일 점수 카드 반응형 수정
```
